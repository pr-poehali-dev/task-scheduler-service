# 📦 Готово к загрузке на хостинг

## ⚠️ ВАЖНО: Сначала нужно собрать проект!

Прежде чем загружать на хостинг, выполните:

```bash
npm install
npm run build
```

Это создаст папку `dist/` с собранным frontend.

---

## 📁 Что загружать на хостинг

После команды `npm run build` загрузите:

### 1. Из папки `dist/` → В корень сайта (public_html/)

```
dist/
├── index.html          → загрузить
├── assets/             → загрузить всю папку
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── ...
└── favicon.ico         → загрузить (если есть)
```

### 2. Из корня проекта → В корень сайта

```
api/                    → загрузить всю папку
config.php              → загрузить и настроить
.htaccess               → загрузить
database.sql            → временно (удалить после импорта)
setup-telegram.php      → временно (удалить после настройки)
```

---

## 🎯 Финальная структура на хостинге

```
public_html/
├── index.html              (из dist/)
├── assets/                 (из dist/)
│   ├── index-abc123.js
│   ├── index-xyz789.css
│   └── ...
├── api/                    (из корня проекта)
│   ├── telegram-webhook.php
│   ├── notify-task.php
│   ├── sync-task.php
│   └── save-task.php
├── config.php              (из корня проекта)
└── .htaccess               (из корня проекта)
```

---

## 🚀 Пошаговая инструкция

### Шаг 1: Локально
```bash
cd taskflow
npm install
npm run build
```

### Шаг 2: Загрузка через FTP

Откройте FileZilla и загрузите:

1. **Содержимое папки dist/** → в корень сайта
   - Все файлы из dist/ (index.html, assets/, и т.д.)

2. **Папку api/** → в корень сайта
   - Вся папка целиком

3. **Файлы config.php и .htaccess** → в корень сайта

4. **Файл database.sql** → временно (удалить после)

### Шаг 3: База данных

1. Откройте phpMyAdmin
2. Создайте БД: `taskflow`
3. Импортируйте `database.sql`

### Шаг 4: Настройка

Откройте `config.php` на хостинге:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'taskflow');
define('DB_USER', 'ваш_mysql_логин');
define('DB_PASS', 'ваш_mysql_пароль');
define('BASE_URL', 'https://ваш-домен.ru');
```

---

## ✅ Проверка

Откройте: `https://ваш-домен.ru`

Войдите:
- Email: `alex@company.ru`
- Пароль: `admin123`

---

## 📌 Важные заметки

1. **Папка dist/ создается только после `npm run build`**
2. Без сборки проекта загружать нечего
3. На хостинг НЕ загружайте: `node_modules/`, `src/`, `.git/`

---

## 🆘 Нет npm/node.js?

Если у вас нет Node.js локально:

1. Установите Node.js с [nodejs.org](https://nodejs.org)
2. Перезапустите терминал
3. Выполните команды выше

Или попросите разработчика собрать проект за вас.

---

**После сборки вернитесь к этой инструкции!**
