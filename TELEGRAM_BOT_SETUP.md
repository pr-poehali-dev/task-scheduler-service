# Настройка Telegram бота для TaskFlow

## 1. Создание бота в Telegram

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "TaskFlow Уведомления")
4. Введите username (должен заканчиваться на "bot", например: "taskflow_notify_bot")
5. Скопируйте токен вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

## 2. Добавление токена в проект

1. В интерфейсе poehali.dev добавьте секрет `TELEGRAM_BOT_TOKEN`
2. Вставьте скопированный токен

## 3. Настройка webhook

После добавления токена выполните следующий запрос для настройки webhook:

```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://functions.poehali.dev/4ce7411a-e9ec-4a29-942e-dd87c76d49d5"}'
```

Замените `<ВАШ_ТОКЕН>` на токен, полученный от BotFather.

## 4. Проверка настройки

Выполните команду для проверки webhook:

```bash
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
```

Вы должны увидеть, что webhook установлен на URL функции.

## 5. Использование бота

### Для пользователей:

1. Зайдите в профиль в веб-приложении
2. Найдите раздел "Telegram уведомления"
3. Нажмите кнопку "Подключить Telegram"
4. Нажмите "Start" в открывшемся боте
5. Готово! Теперь вы будете получать уведомления

### Для администраторов:

1. Создайте задачу и назначьте её пользователю
2. Если у пользователя подключен Telegram, он получит уведомление
3. Пользователь может отметить задачу выполненной прямо в Telegram

## Архитектура решения

### Backend функции:

1. **telegram-bot** - обработчик webhook от Telegram
   - URL: `https://functions.poehali.dev/4ce7411a-e9ec-4a29-942e-dd87c76d49d5`
   - Обрабатывает команды `/start` и callback-кнопки

2. **notify-task** - отправка уведомлений о новых задачах
   - URL: `https://functions.poehali.dev/205b21b6-bd50-438b-9607-dcb2c16f6bee`
   - Вызывается из веб-приложения при создании задачи

3. **sync-task** - синхронизация статуса задач
   - URL: `https://functions.poehali.dev/42bdb13e-b52d-4f65-8979-48a77efc7f23`
   - Синхронизирует статус между Telegram и веб-приложением

4. **save-task** - сохранение задач в БД
   - URL: `https://functions.poehali.dev/e60cfbac-0232-4315-8d37-3c05469d6bc2`
   - Сохраняет задачи и привязки к пользователям

### База данных:

- `users` - пользователи с их telegram_chat_id
- `tasks` - задачи
- `task_assignments` - связь задач с пользователями

## Формат уведомления в Telegram

```
🔥 Новая задача для выполнения

📋 Название: Подготовить отчет
📅 Срок: 2025-01-25
📊 Статус: В работе
👤 От кого: Алексей Иванов

[Кнопка: ✅ Отметить выполненной]
```

После нажатия кнопки задача автоматически отмечается выполненной в базе данных и в веб-приложении.

## Устранение проблем

### Бот не отвечает
- Проверьте, что токен правильно добавлен в секреты
- Проверьте настройку webhook командой `getWebhookInfo`

### Уведомления не приходят
- Убедитесь, что пользователь подключил Telegram через профиль
- Проверьте, что в базе сохранён telegram_chat_id пользователя

### Задачи не отмечаются выполненными
- Проверьте логи backend функции `telegram-bot`
- Убедитесь, что task_id корректно передаётся в callback_data
