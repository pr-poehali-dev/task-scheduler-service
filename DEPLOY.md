# 🚀 Быстрый деплой TaskFlow на хостинг

## Чек-лист перед загрузкой

- [ ] Node.js установлен локально
- [ ] Хостинг с PHP 7.4+ и MySQL
- [ ] Доступ к phpMyAdmin
- [ ] FTP/SFTP доступ

---

## ⚡ Быстрая установка (5 минут)

### 1️⃣ Локально: Соберите frontend

```bash
npm install
npm run build
```

Появится папка `dist/` с готовым сайтом.

---

### 2️⃣ Хостинг: Загрузите файлы

Через FileZilla/FTP загрузите:

```
📂 Из папки dist/ → В корень сайта (public_html/)
   ├── index.html
   ├── assets/
   └── ...

📂 Из корня проекта → В корень сайта
   ├── api/
   ├── config.php
   ├── database.sql (временно)
   ├── setup-telegram.php (временно)
   └── .htaccess
```

---

### 3️⃣ phpMyAdmin: Создайте БД

1. Откройте phpMyAdmin
2. Создайте БД: **taskflow**
3. Кодировка: **utf8mb4_unicode_ci**
4. SQL → Вставьте содержимое `database.sql` → Выполнить

---

### 4️⃣ Настройте config.php

Откройте `config.php` на хостинге и измените:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'taskflow');
define('DB_USER', 'ваш_mysql_логин');
define('DB_PASS', 'ваш_mysql_пароль');
define('BASE_URL', 'https://ваш-домен.ru');
```

---

### 5️⃣ Проверьте работу

Откройте: `https://ваш-домен.ru`

Войдите:
- **Email:** alex@company.ru
- **Пароль:** admin123

✅ **Готово!** Сайт работает!

---

## 🤖 Telegram (опционально, +2 минуты)

### 1. Создайте бота

- Telegram → @BotFather → `/newbot`
- Скопируйте токен

### 2. Добавьте токен

В `config.php`:
```php
define('TELEGRAM_BOT_TOKEN', 'ваш_токен');
```

### 3. Установите webhook

Откройте: `https://ваш-домен.ru/setup-telegram.php?action=set`

✅ **Готово!** Уведомления работают!

---

## 🧹 После установки

Удалите временные файлы:
```bash
rm database.sql
rm setup-telegram.php  # или ограничьте доступ
```

Смените пароли:
- Профиль → Настройки → Новый пароль

---

## 🐛 Если что-то не работает

### Ошибка БД
→ Проверьте `config.php`

### 404 на страницах
→ Проверьте `.htaccess` в корне

### API не отвечает
→ Проверьте, что папка `api/` загружена

### Telegram не работает
→ Нужен HTTPS (Let's Encrypt)

---

## 📋 Файлы для загрузки на хостинг

```
✅ Обязательно:
- dist/ (содержимое)
- api/
- config.php
- .htaccess

📋 Временно (удалить после):
- database.sql
- setup-telegram.php

❌ НЕ загружать:
- node_modules/
- src/
- backend/ (старые Cloud Functions)
- php-version/ (старая версия)
```

---

## ✨ Что дальше?

1. Создайте первую задачу
2. Пригласите команду (Регистрация)
3. Настройте Telegram уведомления
4. Наслаждайтесь! 🎉

---

**Полная инструкция:** [INSTALL.md](INSTALL.md)
