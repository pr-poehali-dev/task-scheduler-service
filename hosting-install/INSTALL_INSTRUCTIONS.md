# 🚀 Инструкция по установке TaskFlow на хостинг

## Быстрая установка за 10 шагов

### Шаг 1: Подготовьте файлы

1. Скачайте все файлы из папки `hosting-install/`
2. Соберите frontend локально:
   ```bash
   npm install
   npm run build
   ```

### Шаг 2: Настройте config.py

Откройте `config.py` и заполните:

```python
TELEGRAM_BOT_TOKEN = "ваш_токен_от_BotFather"
DATABASE_URL = "postgresql://user:pass@host:5432/dbname"
BACKEND_PATH = "/home/username/backend"  # Реальный путь на сервере!
```

### Шаг 3: Обновите CGI скрипты

В каждом файле `cgi-bin/*.py` замените настройки:

```python
BACKEND_PATH = '/home/username/backend'  # Ваш путь
BOT_TOKEN = 'ваш_токен'
DATABASE_URL = 'postgresql://...'
```

### Шаг 4: Загрузите файлы через файловый менеджер

#### 4.1. Frontend (public_html/)
Загрузите из папки `dist/`:
- `index.html`
- `assets/` (вся папка)
- `.htaccess` (из hosting-install/public_html/)

#### 4.2. Backend (cgi-bin/)
Загрузите из `hosting-install/cgi-bin/`:
- `telegram-bot.py`
- `notify-task.py`
- `sync-task.py`
- `save-task.py`

#### 4.3. Backend модули (backend/)
Загрузите из проекта папку `backend/`:
- `backend/telegram-bot/` (со всеми файлами)
- `backend/notify-task/`
- `backend/sync-task/`
- `backend/save-task/`

### Шаг 5: Установите права доступа

Через файловый менеджер:

**CGI скрипты (cgi-bin/*.py):**
- Права: **755** (rwxr-xr-x)
- Как: Выделите файл → ПКМ → Права доступа → 755

**Backend модули (backend/*/index.py):**
- Права: **644** (rw-r--r--)

**Frontend файлы (public_html/*):**
- Файлы: **644**
- Папки: **755**

### Шаг 6: Создайте базу данных PostgreSQL

В панели управления хостингом:

1. Откройте раздел **"PostgreSQL"**
2. Создайте базу:
   - Имя: `taskflow_db`
   - Пользователь: `taskflow_user`
   - Пароль: (придумайте)
3. Назначьте пользователя к базе

### Шаг 7: Примените миграции

Откройте **phpPgAdmin** или **Adminer**:

1. Выберите базу `taskflow_db`
2. Вкладка **SQL**
3. Скопируйте содержимое `db_migrations/V0001__create_initial_schema.sql`
4. Вставьте и выполните

### Шаг 8: Установите Python зависимости

**Если есть SSH:**
```bash
cd /home/username/backend/telegram-bot
pip3 install --user -r requirements.txt

cd ../notify-task
pip3 install --user -r requirements.txt

cd ../sync-task
pip3 install --user -r requirements.txt

cd ../save-task
pip3 install --user -r requirements.txt
```

**Если нет SSH:**
Напишите в поддержку хостинга с просьбой установить:
- `psycopg2-binary`
- `pydantic`

### Шаг 9: Настройте Telegram webhook

Откройте в браузере (замените на свои данные):

```
https://api.telegram.org/botYOUR_TOKEN/setWebhook?url=https://your-domain.com/cgi-bin/telegram-bot.py
```

Ответ должен быть:
```json
{"ok":true,"result":true}
```

### Шаг 10: Проверка

1. Откройте `https://your-domain.com` - должен открыться TaskFlow
2. Откройте `https://your-domain.com/cgi-bin/telegram-bot.py` - должен вернуть JSON
3. В Telegram отправьте боту: `/start test@example.com`
4. Создайте задачу в TaskFlow - проверьте уведомление

---

## 📂 Структура на хостинге

```
/home/username/
├── public_html/              ← Веб-сайт (Frontend)
│   ├── index.html
│   ├── assets/
│   │   ├── index-abc123.js
│   │   ├── index-xyz456.css
│   │   └── ...
│   └── .htaccess
│
├── cgi-bin/                  ← Python CGI скрипты
│   ├── telegram-bot.py
│   ├── notify-task.py
│   ├── sync-task.py
│   └── save-task.py
│
└── backend/                  ← Backend модули
    ├── telegram-bot/
    │   ├── index.py
    │   └── requirements.txt
    ├── notify-task/
    │   ├── index.py
    │   └── requirements.txt
    ├── sync-task/
    │   ├── index.py
    │   └── requirements.txt
    └── save-task/
        ├── index.py
        └── requirements.txt
```

---

## 🔧 Настройка Backend URLs в frontend

Перед сборкой frontend отредактируйте `src/config/backend.ts`:

```typescript
export const BACKEND_URLS = {
  TELEGRAM_BOT: 'https://your-domain.com/cgi-bin/telegram-bot.py',
  NOTIFY_TASK: 'https://your-domain.com/cgi-bin/notify-task.py',
  SAVE_TASK: 'https://your-domain.com/cgi-bin/save-task.py',
  SYNC_TASK: 'https://your-domain.com/cgi-bin/sync-task.py'
} as const;
```

Затем пересоберите:
```bash
npm run build
```

---

## ❗ Важные замечания

### 1. Пути в CGI скриптах

**ОБЯЗАТЕЛЬНО** замените `/home/username/` на реальный путь:

❌ **Неправильно:**
```python
BACKEND_PATH = '/home/username/backend'
```

✅ **Правильно:**
```python
BACKEND_PATH = '/home/mysite123/backend'  # Ваш реальный путь!
```

**Как узнать путь:**
- Создайте файл `test.php` в `public_html/`:
```php
<?php echo $_SERVER['DOCUMENT_ROOT']; ?>
```
- Откройте в браузере: `https://your-domain.com/test.php`
- Увидите: `/home/mysite123/public_html`
- Значит BACKEND_PATH = `/home/mysite123/backend`

### 2. Права доступа

CGI скрипты НЕ будут работать без правильных прав:
- **755** для всех `.py` файлов в `cgi-bin/`
- Без этого будет **500 Internal Server Error**

### 3. Шебанг (первая строка)

Если хостинг использует другой путь к Python, измените первую строку:

```python
#!/usr/bin/env python3  # По умолчанию
```

На некоторых хостингах нужно:
```python
#!/usr/bin/python3
# или
#!/usr/local/bin/python3
```

### 4. HTTPS обязателен

Telegram требует HTTPS для webhook. Убедитесь что:
- Сайт работает по HTTPS
- SSL сертификат валиден (не self-signed)

---

## 🐛 Решение проблем

### Проблема: 500 Internal Server Error

**Причины:**
1. Неправильные права (должно быть 755)
2. Неверный шебанг (первая строка)
3. Синтаксическая ошибка

**Решение:**
1. Проверьте права: ПКМ → Права → 755
2. Проверьте путь к Python: `which python3` через SSH
3. Смотрите логи ошибок в панели хостинга

### Проблема: ModuleNotFoundError: No module named 'psycopg2'

**Причина:** Не установлены зависимости

**Решение:**
```bash
pip3 install --user psycopg2-binary pydantic
```

Или напишите в поддержку хостинга.

### Проблема: can't open file 'index.py'

**Причина:** Неверный путь в BACKEND_PATH

**Решение:**
1. Проверьте, что файл `backend/telegram-bot/index.py` существует
2. Проверьте путь в CGI скрипте
3. Используйте абсолютный путь: `/home/username/backend`

### Проблема: Webhook не работает

**Проверка:**
```
https://api.telegram.org/botTOKEN/getWebhookInfo
```

Должно быть:
```json
{
  "url": "https://your-domain.com/cgi-bin/telegram-bot.py",
  "pending_update_count": 0
}
```

**Если url пустой:**
```
https://api.telegram.org/botTOKEN/setWebhook?url=https://your-domain.com/cgi-bin/telegram-bot.py
```

### Проблема: CORS errors в консоли браузера

**Решение:**
Убедитесь что `.htaccess` загружен в `public_html/` с правильным содержимым.

---

## ✅ Чеклист готовности

Перед запуском проверьте:

- [ ] Frontend собран (`npm run build`)
- [ ] Backend URLs обновлены в `src/config/backend.ts`
- [ ] `config.py` заполнен (токен, база, пути)
- [ ] Все 4 CGI скрипта обновлены с настройками
- [ ] Frontend загружен в `public_html/`
- [ ] CGI скрипты загружены в `cgi-bin/`
- [ ] Backend модули загружены в `backend/`
- [ ] `.htaccess` загружен в `public_html/`
- [ ] Права 755 установлены для CGI скриптов
- [ ] База данных PostgreSQL создана
- [ ] Миграции применены (таблицы созданы)
- [ ] Python зависимости установлены
- [ ] Telegram webhook настроен
- [ ] Frontend открывается в браузере
- [ ] CGI скрипт отвечает (не 404)
- [ ] Бот отвечает на `/start`
- [ ] Тестовая задача создана и уведомление пришло

---

## 📞 Поддержка

Если не получается:
1. Проверьте логи хостинга (error_log)
2. Читайте полную документацию: `INSTALL_VIA_FILE_MANAGER.md`
3. Сообщество: https://t.me/+QgiLIa1gFRY4Y2Iy

**Успехов! 🚀**
