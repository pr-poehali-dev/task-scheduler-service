# 🚀 Установка TaskFlow

## Полная инструкция по развертыванию на хостинге

---

## 📋 Содержание

1. [Требования](#требования)
2. [Структура проекта](#структура-проекта)
3. [Установка за 5 шагов](#установка-за-5-шагов)
4. [Настройка Telegram](#настройка-telegram)
5. [Решение проблем](#решение-проблем)

---

## 🔧 Требования

✅ **Хостинг:**
- PHP 7.4 или выше
- MySQL 5.7 или выше
- Поддержка .htaccess (Apache) или nginx
- HTTPS (для Telegram webhook)

✅ **Локально (для сборки):**
- Node.js 16+ и npm/bun
- Git (опционально)

---

## 📁 Структура проекта

После установки у вас будет:

```
taskflow/
├── dist/              # Собранный React frontend (загружать на хостинг)
├── api/               # PHP backend API
│   ├── telegram-webhook.php
│   ├── notify-task.php
│   ├── sync-task.php
│   └── save-task.php
├── config.php         # Настройки подключения к БД и Telegram
├── database.sql       # SQL для создания БД
├── setup-telegram.php # Утилита настройки Telegram бота
└── .htaccess         # Конфигурация Apache (создать вручную)
```

---

## 🎯 Установка за 5 шагов

### Шаг 1: Соберите frontend локально

```bash
# Склонируйте репозиторий или скачайте проект
cd taskflow

# Установите зависимости
npm install
# или
bun install

# Соберите production билд
npm run build
# или
bun run build
```

После сборки появится папка `dist/` с готовым frontend.

---

### Шаг 2: Загрузите файлы на хостинг

Загрузите через FTP/SFTP следующие файлы и папки:

```
📤 Загрузить на хостинг:
├── dist/              → в корень сайта (содержимое папки!)
├── api/               → в корень сайта
├── config.php         → в корень сайта
├── database.sql       → временно (удалить после импорта)
└── setup-telegram.php → временно (удалить после настройки)
```

⚠️ **ВАЖНО:** Загружайте **содержимое** папки `dist/`, а не саму папку!

**Правильно:**
```
public_html/
├── index.html
├── assets/
├── api/
├── config.php
```

**Неправильно:**
```
public_html/
└── dist/
    ├── index.html
    └── assets/
```

---

### Шаг 3: Создайте БД и импортируйте SQL

1. Откройте **phpMyAdmin** на хостинге
2. Создайте новую базу данных: **taskflow**
3. Выберите кодировку: **utf8mb4_unicode_ci**
4. Перейдите во вкладку **SQL**
5. Скопируйте содержимое файла `database.sql`
6. Вставьте и нажмите **Выполнить**

✅ Вы увидите сообщение: "База данных TaskFlow успешно создана!"

---

### Шаг 4: Настройте config.php

Откройте файл `config.php` на хостинге и измените:

```php
<?php

// 🔧 Настройки базы данных
define('DB_HOST', 'localhost');           // Обычно localhost
define('DB_NAME', 'taskflow');            // Имя БД из Шага 3
define('DB_USER', 'ваш_пользователь');    // Логин MySQL
define('DB_PASS', 'ваш_пароль');          // Пароль MySQL

// 🌐 URL вашего сайта
define('BASE_URL', 'https://ваш-домен.ru');

// 🤖 Токен Telegram бота (заполните позже)
define('TELEGRAM_BOT_TOKEN', '');
```

**Где взять данные MySQL?**
- Обычно хостинг предоставляет их в панели управления
- Проверьте раздел "Базы данных" или "MySQL"

---

### Шаг 5: Создайте .htaccess

Создайте файл `.htaccess` в **корне сайта** (рядом с index.html):

```apache
# Включаем Rewrite Engine
RewriteEngine On

# Перенаправляем API запросы на PHP
RewriteRule ^api/(.*)$ api/$1 [L]

# Все остальные запросы на index.html (для React Router)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} !^/api/
RewriteRule . /index.html [L]
```

Если используете **nginx**, добавьте в конфигурацию:

```nginx
location /api/ {
    try_files $uri $uri/ =404;
}

location / {
    try_files $uri $uri/ /index.html;
}
```

---

## ✅ Готово! Проверка установки

Откройте в браузере:

```
https://ваш-домен.ru
```

Войдите с тестовыми данными:

| Email | Пароль | Роль |
|-------|--------|------|
| alex@company.ru | admin123 | Администратор |
| maria@company.ru | admin123 | Пользователь |

Если видите главную страницу — установка успешна! 🎉

---

## 🤖 Настройка Telegram (опционально)

### 1. Создайте бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot`
4. Придумайте имя (например, "TaskFlow Bot")
5. Придумайте username (например, "my_taskflow_bot")
6. **Скопируйте токен** (например: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Добавьте токен в config.php

```php
define('TELEGRAM_BOT_TOKEN', '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz');
```

### 3. Установите webhook

Откройте в браузере:

```
https://ваш-домен.ru/setup-telegram.php?action=set
```

Вы увидите:
```
✅ Webhook успешно установлен!
📱 Теперь пользователи могут подключить Telegram уведомления
```

### 4. Подключите уведомления (для каждого пользователя)

1. Войдите в TaskFlow
2. Перейдите в **Профиль → Настройки**
3. Нажмите **Подключить Telegram**
4. Откройте ссылку и нажмите **START** в боте

Готово! Теперь при назначении задачи пользователь получит уведомление в Telegram! 📱

---

## 🐛 Решение проблем

### ❌ Ошибка подключения к БД

**Симптом:** "Database connection failed"

**Решение:**
1. Проверьте `config.php` — правильные ли данные?
2. Убедитесь, что БД создана и SQL импортирован
3. Проверьте, что пользователь MySQL имеет права на БД

---

### ❌ 404 Not Found при переходе на страницы

**Симптом:** При обновлении страницы `/tasks` появляется 404

**Решение:**
1. Проверьте наличие `.htaccess` в корне
2. Убедитесь, что `mod_rewrite` включен на хостинге
3. Для nginx настройте `try_files` (см. Шаг 5)

---

### ❌ Telegram не отправляет уведомления

**Симптом:** Задачи создаются, но уведомлений нет

**Решение:**
1. Проверьте webhook: `https://ваш-домен.ru/setup-telegram.php?action=info`
2. Убедитесь, что сайт работает по **HTTPS** (Telegram требует SSL)
3. Проверьте, что пользователь подключил Telegram (в профиле)
4. Проверьте логи хостинга на ошибки PHP

---

### ❌ API запросы возвращают 500 ошибку

**Симптом:** Задачи не сохраняются, ошибки в консоли

**Решение:**
1. Откройте логи PHP на хостинге
2. Проверьте права доступа к файлам API (должны быть 644)
3. Убедитесь, что `config.php` корректно подключается
4. Проверьте версию PHP (должна быть 7.4+)

---

## 📊 Проверка работоспособности

### Проверка frontend:
```
https://ваш-домен.ru
```
✅ Должна загрузиться страница входа

### Проверка API:
```
https://ваш-домен.ru/api/sync-task.php
```
✅ Должен вернуть JSON (возможно пустой `{}`)

### Проверка БД:
Откройте phpMyAdmin → taskflow → Просмотр таблицы `users`
✅ Должны быть 5 тестовых пользователей

---

## 🔒 Безопасность (ВАЖНО!)

После установки **обязательно**:

1. ✅ Удалите файлы:
   - `database.sql`
   - `setup-telegram.php` (или ограничьте доступ)

2. ✅ Смените пароли:
   - Войдите в профиль каждого пользователя
   - Смените пароль с `admin123` на надежный

3. ✅ Настройте SSL:
   - Убедитесь, что сайт работает по HTTPS
   - Получите бесплатный сертификат Let's Encrypt

4. ✅ Ограничьте доступ к `config.php`:
   ```apache
   <Files "config.php">
       Order Allow,Deny
       Deny from all
   </Files>
   ```

---

## 🎉 Готово!

Теперь ваш TaskFlow полностью функционален:

✅ React frontend работает  
✅ PHP backend отвечает на API запросы  
✅ MySQL хранит данные  
✅ Telegram отправляет уведомления  

**Что дальше?**
- Создайте первую задачу
- Пригласите команду
- Настройте Telegram уведомления
- Наслаждайтесь работой! 🚀

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи PHP на хостинге
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все файлы загружены корректно
4. Перепроверьте `config.php`

**Удачи! 🌟**
