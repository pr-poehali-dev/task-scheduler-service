# 📁 Структура проекта TaskFlow

## React Frontend + PHP Backend

---

## 🎯 Финальная структура для деплоя

После сборки (`npm run build`) получаем:

```
taskflow/
│
├── 📂 dist/                      # Собранный frontend (загружать на хостинг)
│   ├── index.html               # Главная страница
│   ├── assets/                  # CSS, JS, изображения
│   │   ├── index-[hash].css
│   │   ├── index-[hash].js
│   │   └── ...
│   └── favicon.ico
│
├── 📂 api/                       # PHP Backend API (загружать на хостинг)
│   ├── telegram-webhook.php     # Webhook для Telegram бота
│   ├── notify-task.php          # Отправка уведомлений
│   ├── sync-task.php            # Синхронизация статусов
│   └── save-task.php            # Сохранение задач в БД
│
├── 📄 config.php                 # Конфигурация БД и Telegram (НАСТРОИТЬ!)
├── 📄 database.sql               # SQL для создания БД
├── 📄 setup-telegram.php         # Утилита настройки бота
├── 📄 .htaccess                  # Apache конфигурация
├── 📄 nginx.conf.example         # Nginx конфигурация (если нужно)
│
├── 📄 INSTALL.md                 # Полная инструкция
├── 📄 DEPLOY.md                  # Быстрый деплой
└── 📄 PROJECT_STRUCTURE.md       # Этот файл
```

---

## 🚀 Что загружать на хостинг

### ✅ Обязательно загрузить:

```
public_html/                     # Корень сайта
├── index.html                   # Из dist/
├── assets/                      # Из dist/
├── favicon.ico                  # Из dist/
├── api/                         # Вся папка
│   ├── telegram-webhook.php
│   ├── notify-task.php
│   ├── sync-task.php
│   └── save-task.php
├── config.php                   # ⚙️ НАСТРОИТЬ перед загрузкой!
└── .htaccess                    # Для Apache
```

### 📋 Временно загрузить (удалить после установки):

```
├── database.sql                 # Импортировать в phpMyAdmin, потом удалить
└── setup-telegram.php           # Настроить бота, потом удалить
```

### ❌ НЕ загружать на хостинг:

```
❌ node_modules/                 # npm зависимости (нужны только локально)
❌ src/                          # Исходники React (нужны только локально)
❌ public/                       # Статика для разработки
❌ .git/                         # Git репозиторий
❌ package.json                  # npm конфигурация
❌ vite.config.ts                # Vite конфигурация
❌ tsconfig.json                 # TypeScript конфигурация
```

---

## 📦 Исходники (для разработки)

```
src/
├── components/                  # React компоненты
│   ├── ui/                     # UI библиотека (shadcn)
│   ├── AuthScreen.tsx          # Авторизация/регистрация
│   ├── DashboardTab.tsx        # Главная страница
│   ├── TasksTab.tsx            # Список задач
│   ├── Sidebar.tsx             # Боковая панель
│   └── ...
│
├── pages/                       # Страницы
│   └── Index.tsx               # Главная страница приложения
│
├── config/                      # Конфигурация
│   └── backend.ts              # URLs для API запросов
│
├── lib/                         # Утилиты
│   └── utils.ts
│
├── App.tsx                      # Главный компонент
├── main.tsx                     # Entry point
└── index.css                    # Глобальные стили
```

---

## 🔄 Процесс разработки и деплоя

### Локально (разработка):

```bash
# 1. Установить зависимости
npm install

# 2. Запустить dev сервер
npm run dev

# 3. Сборка для production
npm run build
```

### На хостинге (деплой):

```bash
# 1. Загрузить файлы через FTP/SFTP
FileZilla → Загрузить dist/ и api/

# 2. Настроить config.php
Через файловый менеджер хостинга

# 3. Импортировать database.sql
phpMyAdmin → Импорт → database.sql

# 4. Проверить работу
https://ваш-домен.ru
```

---

## 🔌 API Endpoints

Все API находятся в папке `/api/`:

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/telegram-webhook.php` | POST | Обработка Telegram webhook |
| `/api/notify-task.php` | POST | Отправка уведомления о задаче |
| `/api/sync-task.php` | GET/POST | Синхронизация статуса задачи |
| `/api/save-task.php` | POST | Сохранение новой задачи |

---

## 💾 База данных

### Таблицы:

- **users** — Пользователи системы
  - id, full_name, email, password_hash, role, telegram_chat_id, tasks_completed

- **tasks** — Задачи
  - id, title, description, priority, status, deadline, created_by, assigned_to

- **notes** — Личные заметки
  - id, user_id, text, completed

### Тестовые данные:

- 5 пользователей (alex@company.ru, maria@company.ru, и т.д.)
- 8 задач с разными статусами
- 5 заметок

Пароль для всех: **admin123**

---

## 🤖 Telegram интеграция

### Файлы:

- `api/telegram-webhook.php` — Принимает обновления от Telegram
- `api/notify-task.php` — Отправляет уведомления пользователям
- `setup-telegram.php` — Утилита для настройки webhook

### Workflow:

```
1. Пользователь создает задачу в веб-интерфейсе
   ↓
2. Frontend отправляет POST на /api/save-task.php
   ↓
3. PHP сохраняет задачу в MySQL
   ↓
4. Frontend отправляет POST на /api/notify-task.php
   ↓
5. PHP находит исполнителя с telegram_chat_id
   ↓
6. PHP отправляет сообщение через Telegram Bot API
   ↓
7. Пользователь получает уведомление в Telegram
```

---

## 🛠️ Технологический стек

### Frontend:
- **React 18** — UI библиотека
- **TypeScript** — Типизация
- **Vite** — Сборщик
- **Tailwind CSS** — Стили
- **shadcn/ui** — Компоненты
- **React Router** — Навигация

### Backend:
- **PHP 7.4+** — Server-side
- **MySQL 5.7+** — База данных
- **PDO** — Работа с БД
- **Telegram Bot API** — Уведомления

### Хостинг требования:
- PHP 7.4+ с PDO MySQL
- MySQL 5.7+
- Apache с mod_rewrite или Nginx
- HTTPS (для Telegram)

---

## 📝 Важные файлы конфигурации

### config.php
```php
// База данных
define('DB_HOST', 'localhost');
define('DB_NAME', 'taskflow');
define('DB_USER', 'root');
define('DB_PASS', '');

// Домен
define('BASE_URL', 'https://ваш-домен.ru');

// Telegram
define('TELEGRAM_BOT_TOKEN', 'токен_бота');
```

### .htaccess (Apache)
- Защита config.php
- Перенаправление API запросов
- React Router (SPA)
- CORS заголовки

### src/config/backend.ts
```typescript
const API_BASE = import.meta.env.VITE_API_URL || '/api';

export const BACKEND_URLS = {
  TELEGRAM_BOT: `${API_BASE}/telegram-webhook.php`,
  NOTIFY_TASK: `${API_BASE}/notify-task.php`,
  SAVE_TASK: `${API_BASE}/save-task.php`,
  SYNC_TASK: `${API_BASE}/sync-task.php`
};
```

---

## ✅ Чек-лист перед деплоем

- [ ] Собрал frontend: `npm run build`
- [ ] Настроил config.php (DB_*, BASE_URL)
- [ ] Загрузил dist/ (содержимое!) в корень
- [ ] Загрузил api/ в корень
- [ ] Загрузил .htaccess в корень
- [ ] Создал БД taskflow в phpMyAdmin
- [ ] Импортировал database.sql
- [ ] Открыл сайт и проверил вход
- [ ] (Опционально) Настроил Telegram бота
- [ ] Удалил database.sql и setup-telegram.php
- [ ] Сменил пароли пользователей

---

## 🎉 Готово!

После выполнения всех шагов ваш TaskFlow полностью работает:

✅ React SPA на frontend  
✅ PHP API на backend  
✅ MySQL для данных  
✅ Telegram для уведомлений  

**Enjoy! 🚀**
