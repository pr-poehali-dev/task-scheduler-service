# ✅ Проект готов к загрузке на хостинг!

## 📁 Папка `ready-to-upload/` содержит все необходимые файлы

### 🚀 Быстрый старт (3 шага):

#### 1️⃣ Скопируйте файлы assets из билда

**Windows:**
```bash
copy-assets-to-upload.bat
```

**Mac/Linux:**
```bash
chmod +x copy-assets-to-upload.sh
./copy-assets-to-upload.sh
```

Это скопирует `assets/`, `favicon.svg`, `placeholder.svg` и `robots.txt` из билда в `ready-to-upload/`.

---

#### 2️⃣ Настройте конфигурацию

Откройте `ready-to-upload/config.php` и укажите:

```php
define('DB_HOST', 'localhost');              // Хост БД
define('DB_NAME', 'taskflow');               // Название БД
define('DB_USER', 'root');                   // Пользователь БД
define('DB_PASS', '');                       // Пароль БД
define('BASE_URL', 'https://ваш-сайт.ru');  // URL вашего сайта
define('TELEGRAM_BOT_TOKEN', '');            // Токен бота (опционально)
```

---

#### 3️⃣ Импортируйте базу данных

1. Зайдите в **phpMyAdmin** на вашем хостинге
2. Создайте базу данных `taskflow` с кодировкой `utf8mb4_unicode_ci`
3. Откройте вкладку **SQL**
4. Скопируйте содержимое файла `ready-to-upload/database.sql`
5. Вставьте и выполните запрос

✅ **База создана!** В ней уже есть 5 тестовых пользователей с паролем `admin123`:
- `alex@company.ru` (admin)
- `maria@company.ru` (user)
- `ivan@company.ru` (user)
- `elena@company.ru` (manager)
- `dmitry@company.ru` (user)

---

### 📤 Загрузка на хостинг

После выполнения шагов выше:

1. Подключитесь к хостингу по FTP/SFTP
2. Загрузите **всё содержимое** папки `ready-to-upload/` в корневую директорию сайта
3. Убедитесь, что структура выглядит так:

```
ваш_сайт.com/
├── index.html
├── assets/              ← Стили и скрипты
├── api/                 ← PHP API
│   ├── telegram-webhook.php
│   ├── notify-task.php
│   ├── sync-task.php
│   └── save-task.php
├── config.php           ← Конфигурация
├── .htaccess            ← Apache настройки
├── favicon.svg          ← Иконка
├── robots.txt           ← SEO
└── setup-telegram.php   ← Настройка бота (опционально)
```

---

### ✅ Проверка работы

1. Откройте ваш сайт в браузере
2. Войдите с логином: `alex@company.ru` / пароль: `admin123`
3. Проверьте функционал:
   - ✅ Дашборд отображается
   - ✅ Задачи создаются
   - ✅ Статистика работает
   - ✅ Дизайн сохранён полностью

---

### 📖 Дополнительные инструкции

- **`ready-to-upload/НАЧАТЬ_ОТСЮДА.txt`** — краткая инструкция
- **`ready-to-upload/ИНСТРУКЦИЯ.txt`** — подробная инструкция
- **`ready-to-upload/setup-telegram.php`** — настройка Telegram бота (опционально)

---

### 🆘 Что делать если не работает?

1. **Проверьте `config.php`** — правильно ли указаны данные БД и BASE_URL
2. **Проверьте базу данных** — импортирован ли `database.sql`
3. **Проверьте `.htaccess`** — загружен ли файл на хостинг
4. **Проверьте права на файлы:**
   - Папки: `755`
   - Файлы: `644`
   - Папка `api/`: `755` (PHP должен иметь доступ)

5. **Проверьте логи хостинга** — часто там видны ошибки PHP

---

### 📞 Настройка Telegram бота (опционально)

Если хотите получать уведомления в Telegram:

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Добавьте токен в `config.php` → `TELEGRAM_BOT_TOKEN`
4. Откройте в браузере: `https://ваш-сайт.ru/setup-telegram.php?action=set`
5. Webhook установлен! Теперь пользователи могут подключать уведомления.

---

## 🎉 Всё готово!

Ваш проект полностью готов к работе на хостинге с:
- ✅ React фронтендом
- ✅ PHP бэкендом
- ✅ MySQL базой данных
- ✅ Telegram ботом
- ✅ Полным дизайном и функционалом

**Успехов! 🚀**
