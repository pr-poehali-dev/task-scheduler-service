# 📦 Установка TaskFlow PHP через phpMyAdmin

> Пошаговая инструкция для установки на любой хостинг с PHP и MySQL

---

## ✅ Что нужно перед началом

- ✅ Хостинг с **PHP 7.4+** и **MySQL 5.7+**
- ✅ Доступ к **панели управления** (cPanel, ISPmanager и т.д.)
- ✅ **phpMyAdmin** для работы с базой данных
- ✅ **Файловый менеджер** или FTP доступ
- ✅ **Telegram бот токен** от @BotFather
- ✅ **HTTPS** на вашем домене (обязательно для Telegram)

---

## 🚀 Шаг 1: Создание базы данных MySQL

### 1.1. Откройте phpMyAdmin

В панели управления хостингом найдите **"Базы данных"** → **"phpMyAdmin"**

### 1.2. Создайте новую базу данных

1. В левом меню нажмите **"Новая"** (New)
2. Введите имя базы: `taskflow_db`
3. Выберите кодировку: `utf8mb4_unicode_ci`
4. Нажмите **"Создать"** (Create)

![Создание БД](https://via.placeholder.com/800x200/4CAF50/FFFFFF?text=phpMyAdmin+%E2%86%92+%D0%9D%D0%BE%D0%B2%D0%B0%D1%8F+%E2%86%92+taskflow_db+%E2%86%92+%D0%A1%D0%BE%D0%B7%D0%B4%D0%B0%D1%82%D1%8C)

### 1.3. Создайте пользователя базы данных (если нужно)

Если на хостинге требуется отдельный пользователь:

1. Перейдите в **"Привилегии"** (Privileges)
2. Нажмите **"Добавить пользователя"** (Add user account)
3. Заполните:
   - **Имя:** `taskflow_user`
   - **Хост:** `localhost`
   - **Пароль:** (сгенерируйте и сохраните!)
4. В разделе **"Привилегии базы данных"** отметьте **ВСЕ** привилегии
5. Нажмите **"Выполнить"** (Go)

---

## 🗄️ Шаг 2: Импорт схемы базы данных

### 2.1. Откройте базу данных

В левом меню phpMyAdmin выберите `taskflow_db`

### 2.2. Перейдите на вкладку SQL

![SQL Tab](https://via.placeholder.com/800x100/2196F3/FFFFFF?text=phpMyAdmin+%E2%86%92+taskflow_db+%E2%86%92+%D0%92%D0%BA%D0%BB%D0%B0%D0%B4%D0%BA%D0%B0+SQL)

### 2.3. Скопируйте содержимое schema.sql

1. Откройте файл `database/schema.sql` в текстовом редакторе
2. Скопируйте **ВЕСЬ** код (Ctrl+A, Ctrl+C)
3. Вставьте в поле SQL в phpMyAdmin (Ctrl+V)

### 2.4. Выполните SQL запрос

Нажмите кнопку **"Выполнить"** (Go) в правом нижнем углу

✅ **Результат:** Должно появиться сообщение об успехе и список созданных таблиц:

```
✓ Создано 8 таблиц
✓ Добавлено 3 пользователя
✓ Добавлено 3 тестовые задачи
✓ Создано 2 представления
✓ Создано 3 хранимые процедуры
✓ Создано 2 события
```

### 2.5. Проверьте таблицы

В левом меню раскройте `taskflow_db` - должно быть **8 таблиц**:

```
📁 taskflow_db
 ├─ 📄 users
 ├─ 📄 tasks
 ├─ 📄 task_assignments
 ├─ 📄 task_comments
 ├─ 📄 notifications
 ├─ 📄 telegram_messages
 ├─ 📄 sessions
 └─ 📄 ...
```

---

## 📁 Шаг 3: Загрузка файлов на хостинг

### 3.1. Откройте файловый менеджер

В панели управления хостингом найдите **"Файлы"** → **"Файловый менеджер"**

### 3.2. Перейдите в корневую директорию

Обычно это:
- `public_html/` (для основного домена)
- `www/` (на некоторых хостингах)
- `htdocs/` (на Apache хостингах)

### 3.3. Удалите стандартные файлы (если есть)

Удалите дефолтные файлы хостинга:
- `index.html`
- `default.html`
- `coming-soon.html`

⚠️ **Не удаляйте папки:** `.well-known`, `cgi-bin`

### 3.4. Загрузите файлы проекта

**Вариант A: Через ZIP архив (рекомендуется)**

1. На вашем компьютере создайте ZIP архив из папки `php-version/`:
   ```
   taskflow.zip содержит:
   ├── api/
   ├── database/
   ├── includes/
   ├── config.php
   ├── index.php
   ├── .htaccess
   └── ...
   ```

2. В файловом менеджере нажмите **"Загрузить"** (Upload)
3. Выберите `taskflow.zip`
4. После загрузки кликните ПКМ на архиве → **"Извлечь"** (Extract)
5. Извлеките в текущую директорию
6. Удалите архив

**Вариант B: Через FTP**

1. Используйте FileZilla или другой FTP клиент
2. Подключитесь к хостингу (данные от хостера)
3. Перейдите в `public_html/`
4. Перетащите все файлы из `php-version/`

### 3.5. Проверьте структуру

Должно получиться:

```
public_html/
├── api/
│   ├── auth.php
│   ├── tasks.php
│   └── telegram-webhook.php
├── database/
│   └── schema.sql
├── includes/
│   ├── header.php
│   └── footer.php
├── logs/ (создастся автоматически)
├── config.php ← ВАЖНО!
├── index.php
├── login.php
├── .htaccess ← ВАЖНО!
└── README.md
```

---

## ⚙️ Шаг 4: Настройка конфигурации

### 4.1. Откройте config.php

В файловом менеджере кликните на `config.php` → **"Редактировать"** (Edit)

### 4.2. Заполните настройки базы данных

Найдите строки:

```php
// Конфигурация базы данных MySQL
define('DB_HOST', 'localhost');
define('DB_NAME', 'taskflow_db');
define('DB_USER', 'taskflow_user');
define('DB_PASS', 'your_password_here');
```

Замените на **ваши** данные:

```php
define('DB_HOST', 'localhost');           // Обычно localhost
define('DB_NAME', 'taskflow_db');         // Имя БД (из шага 1)
define('DB_USER', 'username_taskflow');   // Пользователь БД
define('DB_PASS', 'secure_password_123'); // Пароль БД
```

💡 **Как узнать эти данные?**
- В панели хостинга → "Базы данных" → список баз данных
- Или спросите у техподдержки хостинга

### 4.3. Укажите URL вашего сайта

```php
define('BASE_URL', 'https://your-domain.com');
```

Замените на:

```php
define('BASE_URL', 'https://taskflow.example.com'); // БЕЗ слеша в конце!
```

### 4.4. Сгенерируйте секретный ключ

```php
define('JWT_SECRET', 'your_secret_key_change_this_in_production');
```

Замените на случайную строку (32+ символа):

```php
define('JWT_SECRET', 'a8f9d2e1c4b7f3a6e9d8c2b5f1a4e7d3');
```

💡 **Как сгенерировать:**
- Онлайн: https://www.random.org/strings/
- Или используйте: `openssl rand -hex 32`

### 4.5. Сохраните файл

Нажмите **"Сохранить изменения"** (Save Changes)

---

## 🤖 Шаг 5: Создание Telegram бота

### 5.1. Откройте Telegram

Найдите бота **@BotFather**

### 5.2. Создайте нового бота

Отправьте команду:
```
/newbot
```

BotFather спросит:
```
Alright, a new bot. How are we going to call it? 
Please choose a name for your bot.
```

Введите **имя** бота (например):
```
TaskFlow Notifications
```

BotFather спросит **username**:
```
Good. Now let's choose a username for your bot. 
It must end in `bot`. Like this, for example: TetrisBot or tetris_bot.
```

Введите **username** (должен быть уникальным):
```
taskflow_notify_bot
```

### 5.3. Скопируйте токен

BotFather пришлёт токен:

```
Done! Congratulations on your new bot. 

Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456

Keep your token secure and store it safely, 
it can be used by anyone to control your bot.
```

**📋 Скопируйте токен!**

### 5.4. Добавьте токен в config.php

Снова откройте `config.php` → **Редактировать**

Найдите:
```php
define('TELEGRAM_BOT_TOKEN', 'YOUR_TELEGRAM_BOT_TOKEN');
```

Замените на:
```php
define('TELEGRAM_BOT_TOKEN', '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456');
```

**Сохраните файл!**

---

## 🔗 Шаг 6: Настройка Telegram webhook

### 6.1. Сформируйте URL для настройки

Замените в этом URL:
- `YOUR_TOKEN` → ваш токен от BotFather
- `your-domain.com` → ваш домен

```
https://api.telegram.org/botYOUR_TOKEN/setWebhook?url=https://your-domain.com/api/telegram-webhook.php
```

**Пример:**
```
https://api.telegram.org/bot1234567890:ABCdef.../setWebhook?url=https://taskflow.example.com/api/telegram-webhook.php
```

### 6.2. Откройте URL в браузере

Скопируйте сформированный URL и откройте в новой вкладке браузера.

✅ **Правильный ответ:**
```json
{
  "ok": true,
  "result": true,
  "description": "Webhook was set"
}
```

❌ **Ошибка:**
```json
{
  "ok": false,
  "error_code": 400,
  "description": "Bad Request: bad webhook: HTTPS url must be provided for webhook"
}
```
→ Проверьте что ваш сайт работает по HTTPS!

### 6.3. Проверьте статус webhook

Откройте (замените YOUR_TOKEN):
```
https://api.telegram.org/botYOUR_TOKEN/getWebhookInfo
```

Должен вернуть:
```json
{
  "ok": true,
  "result": {
    "url": "https://your-domain.com/api/telegram-webhook.php",
    "has_custom_certificate": false,
    "pending_update_count": 0,
    "max_connections": 40
  }
}
```

✅ Если `url` не пустой - webhook настроен!

---

## 🎉 Шаг 7: Проверка работы

### 7.1. Откройте сайт в браузере

Перейдите на: `https://your-domain.com`

Должна открыться страница входа:

![Login Page](https://via.placeholder.com/800x400/667eea/FFFFFF?text=TaskFlow+Login+Page)

### 7.2. Войдите с тестовым аккаунтом

Используйте один из тестовых аккаунтов:

**Админ:**
- Email: `admin@taskflow.com`
- Пароль: `admin123`

**Менеджер:**
- Email: `manager@taskflow.com`
- Пароль: `manager123`

**Пользователь:**
- Email: `user@taskflow.com`
- Пароль: `user123`

### 7.3. Проверьте дашборд

После входа должен открыться дашборд со статистикой:

```
┌─────────────────────────────────────┐
│  Добро пожаловать, Администратор!   │
├─────────────────────────────────────┤
│  📊 Статистика:                     │
│  ⏳ Ожидают: 2                      │
│  🔄 В работе: 1                     │
│  ✅ Выполнено: 0                    │
│  ⚠️  Просрочено: 0                   │
└─────────────────────────────────────┘
```

### 7.4. Проверьте Telegram бота

1. В Telegram найдите вашего бота (по username)
2. Нажмите **Start**
3. Отправьте команду:
   ```
   /start admin@taskflow.com
   ```

Бот должен ответить:
```
✅ Отлично! Ваш Telegram привязан к аккаунту Администратор.

Теперь вы будете получать уведомления о новых задачах.

Доступные команды:
/tasks - Мои активные задачи
/help - Помощь
/unlink - Отвязать аккаунт
```

### 7.5. Создайте тестовую задачу

1. В веб-интерфейсе нажмите **"Создать задачу"**
2. Заполните:
   - **Название:** Тестовая задача
   - **Приоритет:** Высокий
   - **Назначено:** (выберите себя)
3. Нажмите **"Создать"**

✅ **Проверьте Telegram** - должно прийти уведомление!

---

## ✅ Шаг 8: Готово!

Поздравляем! 🎉 TaskFlow успешно установлен и работает!

### Что дальше?

1. **Создайте своих пользователей:**
   - Перейдите в **"Пользователи"** (для админа)
   - Или используйте **"Регистрация"** на странице входа

2. **Настройте Telegram уведомления:**
   - **Профиль** → Telegram
   - Привяжите бота: `/start ваш@email.com`

3. **Создайте задачи:**
   - **Задачи** → **Создать задачу**
   - Назначьте на членов команды

4. **Измените тестовые пароли:**
   - **Профиль** → **Изменить пароль**
   - Обязательно смените пароли для безопасности!

5. **Настройте резервное копирование:**
   - В cPanel/ISPmanager настройте автобэкап БД

---

## 🐛 Решение проблем

### Проблема: "Database connection failed"

**Причина:** Неверные данные в config.php

**Решение:**
1. Проверьте `DB_HOST`, `DB_NAME`, `DB_USER`, `DB_PASS`
2. Убедитесь что база данных создана
3. Проверьте что пользователь имеет права на БД

### Проблема: "500 Internal Server Error"

**Причина:** Ошибка в PHP коде или неверные права

**Решение:**
1. Проверьте `logs/error.log`
2. Убедитесь что права файлов: 644, папок: 755
3. Проверьте что `.htaccess` загружен

### Проблема: Telegram бот не отвечает

**Причина:** Webhook не настроен или URL неверный

**Решение:**
1. Проверьте webhook:
   ```
   https://api.telegram.org/botTOKEN/getWebhookInfo
   ```
2. Если `url` пустой - настройте заново (шаг 6)
3. Убедитесь что сайт работает по HTTPS

### Проблема: Уведомления не приходят

**Причина:** Telegram не привязан или токен неверный

**Решение:**
1. В боте отправьте: `/start ваш@email.com`
2. Проверьте `config.php` - правильный ли токен
3. В БД проверьте: `SELECT * FROM users WHERE telegram_chat_id IS NOT NULL`

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте логи:**
   - `logs/error.log` (в файловом менеджере)
   - Логи Apache/PHP в панели хостинга

2. **Сообщество:**
   - https://t.me/+QgiLIa1gFRY4Y2Iy

3. **Документация:**
   - Читайте `README.md` для деталей

---

**Успешной работы с TaskFlow! 🚀**
