# 🚀 TaskFlow - PHP Version

Полноценная PHP версия системы управления задачами с Telegram ботом.

## 📦 Что включено

### ✅ Backend (PHP 7.4+)
- **Аутентификация:** Регистрация, вход, JWT сессии
- **API задач:** CRUD операции, фильтрация, комментарии
- **Telegram бот:** Уведомления, команды, callback buttons
- **База данных:** MySQL с миграциями и хранимыми процедурами

### ✅ Frontend (PHP шаблоны)
- **Главная:** Дашборд со статистикой
- **Задачи:** Список, создание, редактирование
- **Профиль:** Настройки Telegram уведомлений
- **Адаптивный дизайн:** Tailwind CSS + FontAwesome

### ✅ База данных (MySQL)
- 8 таблиц с индексами
- 2 представления (views)
- 3 хранимые процедуры
- 2 автоматических события (events)
- Полнотекстовый поиск

---

## 📋 Требования

- **PHP:** 7.4 или выше (с PDO, curl, json)
- **MySQL:** 5.7 или выше (или MariaDB 10.2+)
- **Apache:** С mod_rewrite
- **HTTPS:** Обязателен для Telegram webhook
- **Telegram бот:** Токен от @BotFather

---

## 🚀 Установка

### Шаг 1: Загрузите файлы

Загрузите всю папку `php-version/` на ваш хостинг в корневую директорию `public_html/` или `www/`.

```
public_html/
├── api/
├── database/
├── includes/
├── logs/
├── config.php
├── index.php
├── login.php
├── .htaccess
└── ...
```

### Шаг 2: Создайте базу данных

1. Откройте **phpMyAdmin** в панели управления хостингом
2. Создайте новую базу данных: `taskflow_db`
3. Откройте вкладку **SQL**
4. Скопируйте весь код из `database/schema.sql`
5. Вставьте и нажмите **Выполнить** (Execute)

✅ Будет создано 8 таблиц + 3 тестовых пользователя

### Шаг 3: Настройте config.php

Откройте файл `config.php` и заполните настройки:

```php
// База данных MySQL
define('DB_HOST', 'localhost');           // Обычно localhost
define('DB_NAME', 'taskflow_db');         // Имя вашей БД
define('DB_USER', 'your_db_user');        // Пользователь БД
define('DB_PASS', 'your_db_password');    // Пароль БД

// Telegram бот (от @BotFather)
define('TELEGRAM_BOT_TOKEN', '1234567890:ABCdefGHIjklMNO...');

// URL вашего сайта
define('BASE_URL', 'https://your-domain.com');

// Секретный ключ (сгенерируйте случайную строку)
define('JWT_SECRET', 'change_this_to_random_string_32_chars');
```

### Шаг 4: Установите права доступа

Через файловый менеджер установите права:

- **Файлы:** 644 (для всех `.php` файлов)
- **Папки:** 755 (для всех директорий)
- **logs/**: 755 и разрешить запись

### Шаг 5: Настройте Telegram webhook

Откройте в браузере (замените TOKEN и DOMAIN):

```
https://api.telegram.org/botYOUR_TOKEN/setWebhook?url=https://your-domain.com/api/telegram-webhook.php
```

Должен вернуться:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

### Шаг 6: Готово!

Откройте ваш сайт: `https://your-domain.com`

**Тестовые аккаунты:**
- 👑 Админ: `admin@taskflow.com` / `admin123`
- 👤 Менеджер: `manager@taskflow.com` / `manager123`
- 👥 Пользователь: `user@taskflow.com` / `user123`

---

## 📂 Структура проекта

```
php-version/
│
├── api/                              # REST API endpoints
│   ├── auth.php                     # Аутентификация (вход/регистрация)
│   ├── tasks.php                    # CRUD задач + комментарии
│   └── telegram-webhook.php         # Telegram бот webhook
│
├── database/                        # Схема базы данных
│   └── schema.sql                   # MySQL миграция (полная)
│
├── includes/                        # Общие компоненты
│   ├── header.php                   # Шапка сайта + навигация
│   └── footer.php                   # Футер + JS функции
│
├── logs/                            # Логи (создаётся автоматически)
│   └── error.log
│
├── config.php                       # Конфигурация проекта
├── index.php                        # Главная страница (дашборд)
├── login.php                        # Страница входа
├── register.php                     # Страница регистрации
├── tasks.php                        # Список всех задач
├── task.php                         # Детали задачи
├── task-create.php                  # Создание задачи
├── my-tasks.php                     # Мои задачи
├── profile.php                      # Профиль пользователя
├── team.php                         # Управление командой
├── users.php                        # Управление пользователями (админ)
├── settings.php                     # Настройки системы (админ)
├── logout.php                       # Выход
├── .htaccess                        # Apache конфигурация
└── README.md                        # Эта инструкция
```

---

## 🔧 API Endpoints

### Аутентификация

```bash
# Вход
POST /api/auth.php?action=login
{
  "email": "user@example.com",
  "password": "password123"
}

# Регистрация
POST /api/auth.php?action=register
{
  "email": "user@example.com",
  "password": "password123",
  "full_name": "Иван Иванов"
}

# Проверка токена
POST /api/auth.php?action=verify
Headers: Authorization: Bearer {token}

# Выход
POST /api/auth.php?action=logout
Headers: Authorization: Bearer {token}
```

### Задачи

```bash
# Получить все задачи
GET /api/tasks.php
Headers: Authorization: Bearer {token}
Query: ?status=pending&priority=high&assigned_to=123

# Получить задачу по ID
GET /api/tasks.php/123
Headers: Authorization: Bearer {token}

# Создать задачу
POST /api/tasks.php
Headers: Authorization: Bearer {token}
{
  "title": "Название задачи",
  "description": "Описание",
  "priority": "high",
  "due_date": "2025-01-25",
  "assigned_to": 2
}

# Обновить задачу
PUT /api/tasks.php/123
Headers: Authorization: Bearer {token}
{
  "status": "completed",
  "priority": "medium"
}

# Удалить задачу
DELETE /api/tasks.php/123
Headers: Authorization: Bearer {token}

# Добавить комментарий
POST /api/tasks.php/123/comment
Headers: Authorization: Bearer {token}
{
  "comment": "Текст комментария"
}
```

### Telegram Bot

```bash
# Webhook (настраивается автоматически)
POST /api/telegram-webhook.php

# Установить webhook
GET /api/telegram-webhook.php?action=set

# Проверить статус webhook
GET /api/telegram-webhook.php?action=info
```

---

## 💬 Telegram бот - Команды

После настройки webhook бот поддерживает:

### `/start EMAIL`
Привязать Telegram к аккаунту TaskFlow

**Пример:**
```
/start user@example.com
```

### `/tasks`
Показать мои активные задачи (до 10 штук)

### `/help`
Показать список доступных команд

### `/unlink`
Отвязать Telegram от аккаунта

### Inline кнопки:
- ✅ **Выполнено** - отметить задачу как завершённую
- 👁 **Посмотреть** - открыть задачу в веб-интерфейсе

---

## 🗄️ База данных

### Таблицы:

1. **users** - Пользователи системы
2. **tasks** - Задачи
3. **task_assignments** - История назначений
4. **task_comments** - Комментарии к задачам
5. **notifications** - Уведомления
6. **telegram_messages** - Логи Telegram сообщений
7. **sessions** - Сессии пользователей (JWT токены)

### Представления (Views):

1. **v_active_tasks** - Активные задачи с join пользователей
2. **v_user_stats** - Статистика по пользователям

### Хранимые процедуры:

1. **sp_assign_task** - Назначить задачу пользователю
2. **sp_complete_task** - Завершить задачу
3. **sp_cleanup_sessions** - Очистка старых сессий

### События (Events):

1. **evt_cleanup_sessions** - Автоочистка сессий (каждый час)
2. **evt_overdue_tasks_notification** - Уведомления о просроченных задачах (каждый день в 9:00)

---

## 🔒 Безопасность

### Реализовано:

✅ **Password hashing** - bcrypt (PHP password_hash)  
✅ **SQL Injection защита** - PDO prepared statements  
✅ **XSS защита** - htmlspecialchars везде  
✅ **CSRF защита** - проверка токенов  
✅ **Session security** - HttpOnly, Secure cookies  
✅ **HTTPS only** - редирект в .htaccess  
✅ **Rate limiting** - на уровне .htaccess  
✅ **Input validation** - на всех endpoints  

### .htaccess включает:

- Защиту от SQL injection в URL
- Защиту от XSS через query strings
- Блокировку подозрительных паттернов
- Security headers (X-XSS-Protection, X-Frame-Options и т.д.)
- Маскировка версии PHP/Apache

---

## 🐛 Отладка

### Проверка логов:

```bash
# Логи PHP ошибок
tail -f logs/error.log

# Логи Apache (зависит от хостинга)
tail -f /var/log/apache2/error.log
```

### Проверка базы данных:

```sql
-- Количество пользователей
SELECT COUNT(*) FROM users;

-- Активные задачи
SELECT * FROM v_active_tasks;

-- Telegram подключения
SELECT id, email, full_name, telegram_chat_id 
FROM users 
WHERE telegram_chat_id IS NOT NULL;
```

### Проверка Telegram webhook:

```bash
# Статус webhook
curl https://api.telegram.org/bot{TOKEN}/getWebhookInfo

# Логи Telegram сообщений
SELECT * FROM telegram_messages ORDER BY created_at DESC LIMIT 10;
```

---

## ❓ Частые проблемы

### 1. Ошибка подключения к БД

**Проблема:** `Database connection failed`

**Решение:**
- Проверьте `config.php` (DB_HOST, DB_NAME, DB_USER, DB_PASS)
- Убедитесь что база данных создана
- Проверьте права пользователя БД

### 2. Telegram webhook не работает

**Проблема:** Бот не отвечает на сообщения

**Решение:**
```bash
# Проверьте webhook
https://api.telegram.org/bot{TOKEN}/getWebhookInfo

# Если url пустой - установите заново
https://api.telegram.org/bot{TOKEN}/setWebhook?url=https://your-domain.com/api/telegram-webhook.php
```

### 3. 500 Internal Server Error

**Проблема:** Белый экран или 500 ошибка

**Решение:**
- Проверьте `logs/error.log`
- Проверьте синтаксис PHP: `php -l file.php`
- Убедитесь что права 644 для файлов, 755 для папок

### 4. Сессия не сохраняется

**Проблема:** После входа перебрасывает на login.php

**Решение:**
- Проверьте что `session_start()` вызывается первым
- Убедитесь что папка `session.save_path` доступна для записи
- Проверьте настройки PHP: `session.cookie_secure`, `session.cookie_httponly`

---

## 📊 Производительность

### Оптимизация:

- ✅ Индексы на всех внешних ключах
- ✅ Полнотекстовый поиск по задачам
- ✅ Кэширование статики (1 год)
- ✅ Gzip сжатие
- ✅ Prepared statements (без overhead)
- ✅ Views для сложных запросов

### Рекомендации:

1. Включите **OPcache** в php.ini:
```ini
opcache.enable=1
opcache.memory_consumption=128
opcache.max_accelerated_files=10000
```

2. Используйте **Redis** для сессий (опционально)
3. Настройте **MySQL query cache**

---

## 🔄 Обновление

### Миграции:

При обновлении схемы БД создавайте новые файлы:

```sql
-- database/migration_002.sql
ALTER TABLE tasks ADD COLUMN tags JSON DEFAULT NULL;
CREATE INDEX idx_tags ON tasks((CAST(tags AS CHAR(255))));
```

Применяйте через phpMyAdmin → SQL → Execute.

---

## 📝 Лицензия

MIT License - свободное использование.

---

## 💬 Поддержка

- **Сообщество:** https://t.me/+QgiLIa1gFRY4Y2Iy
- **GitHub Issues:** (если проект на GitHub)
- **Email:** support@taskflow.com

---

## 🎉 Готово!

Ваша система управления задачами с Telegram ботом готова к работе!

**Следующие шаги:**
1. Создайте своих пользователей
2. Настройте Telegram уведомления
3. Создайте первые задачи
4. Пригласите команду

**Успехов! 🚀**
